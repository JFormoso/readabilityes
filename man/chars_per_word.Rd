% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avg_metrics.R
\name{chars_per_word}
\alias{chars_per_word}
\title{Caracteres promedio por palabra}
\usage{
chars_per_word(text, na_as_zero = FALSE, drop_empty = TRUE, ...)
}
\arguments{
\item{text}{Vector de caracteres con el/los texto(s) a analizar.}

\item{na_as_zero}{Lógico; si es \code{TRUE}, cuando \code{text} tiene \code{NA}
devuelve 0 en lugar de \code{NA}. Además, si un elemento no contiene palabras,
devuelve 0 en lugar de \code{NA}.}

\item{drop_empty}{Lógico; si es \code{TRUE}, descarta tokens vacíos antes de contar.}

\item{...}{Argumentos adicionales que se pasan a \code{\link{tokenize_words}}.}
}
\value{
Un vector numérico con el promedio de caracteres por palabra.
}
\description{
Calcula el promedio de \strong{caracteres} por palabra para cada elemento de
\code{text}. Por defecto, se contabilizan todos los caracteres presentes en
cada token (tal como fueron devueltos por \code{\link{tokenize_words}}),
por lo que normalmente no incluye puntuación si \code{strip_punct = TRUE}.
}
\details{
El conteo de caracteres se realiza con \code{nchar(..., type = "chars")}.
Si deseás excluir explícitamente ciertos signos (p. ej., guiones), asegurate
de configurar \code{strip_punct} o \code{keep_hyphens} en
\code{\link{tokenize_words}} según corresponda.
}
\examples{
chars_per_word("Hola mundo!")                 # típico: ~4 y ~5
chars_per_word("Palabra-compuesta ejemplo")   # depende de `keep_hyphens`

}
\seealso{
\code{\link{tokenize_words}}, \code{\link{count_words}}
}
