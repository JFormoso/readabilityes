% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count-syllables.R
\name{count_syllables}
\alias{count_syllables}
\title{Contar sílabas en un texto}
\usage{
count_syllables(
  text,
  lang = syll_get_option("lang"),
  syllable_fun = syll_count,
  na_as_zero = FALSE,
  drop_empty = TRUE,
  syll_args = list(),
  ...
)
}
\arguments{
\item{text}{Vector de caracteres con el/los texto(s) a analizar.}

\item{lang}{Código de idioma: \code{"es"} (español, por defecto) o \code{"ca"}
(catalán). Se pasa a \code{\link{tokenize_words}} y a
\code{syllable_fun}. Si no se especifica, se usa
\code{syll_get_option("lang")}.}

\item{syllable_fun}{Función que recibe una palabra y devuelve el número de
sílabas. Por defecto \code{syll_count}. Debe aceptar un argumento \code{lang}
si se quiere usar con múltiples idiomas.}

\item{na_as_zero}{Lógico; si es \code{TRUE}, los \code{NA} en \code{text}
devuelven 0 en lugar de \code{NA}.}

\item{drop_empty}{Lógico; si es \code{TRUE}, descarta tokens vacíos antes
de contar.}

\item{syll_args}{Lista de argumentos adicionales para \code{syllable_fun},
además de \code{lang} (que se pasa automáticamente).}

\item{...}{Argumentos adicionales para \code{\link{tokenize_words}}.}
}
\value{
Vector entero con la cantidad total de sílabas por elemento de
\code{text}.
}
\description{
Calcula la cantidad total de sílabas por cada elemento de un vector de
texto. Primero tokeniza con \code{\link{tokenize_words}} y luego suma las
sílabas de cada palabra con \code{syllable_fun} (por defecto,
\code{syll_count}).
}
\examples{
count_syllables("Este es un texto de prueba.")
count_syllables("L'Anna va a l'escola", lang = "ca")
count_syllables(c("Uno dos", NA), na_as_zero = TRUE)

}
\seealso{
\code{\link{tokenize_words}}, \code{\link{syll_count}}
}
