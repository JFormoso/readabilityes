% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avg_metrics.R
\name{avg_syllables_per_word}
\alias{avg_syllables_per_word}
\title{Sílabas promedio por palabra}
\usage{
avg_syllables_per_word(
  text,
  na_as_zero = FALSE,
  drop_empty = TRUE,
  syllable_fun = syll_count,
  syll_args = list(),
  ...
)
}
\arguments{
\item{text}{Vector de caracteres con el/los texto(s) a analizar.}

\item{na_as_zero}{Lógico; si es \code{TRUE}, cuando \code{text} tiene \code{NA}
devuelve 0 en lugar de \code{NA}. Además, si un elemento no contiene palabras,
devuelve 0 en lugar de \code{NA}.}

\item{drop_empty}{Lógico; si es \code{TRUE}, descarta tokens vacíos antes de
contar (se reenvía a las funciones de conteo/tokenización).}

\item{syllable_fun}{Función de silabificación por palabra (por defecto
\code{syll_count}); se reenvía a \code{\link{count_syllables}}.}

\item{syll_args}{Lista de argumentos adicionales para \code{syllable_fun}.}

\item{...}{Argumentos adicionales que se pasan a \code{\link{tokenize_words}}.}
}
\value{
Un vector numérico con el promedio de sílabas por palabra.
}
\description{
Calcula el promedio de sílabas por palabra para cada elemento de \code{text}.
Internamente suma las sílabas con \code{\link{count_syllables}} y divide por
la cantidad de palabras con \code{\link{count_words}}.
}
\examples{
avg_syllables_per_word("Este es un texto de prueba.")
avg_syllables_per_word(c("Una sola", "Dos palabras más"))

}
\seealso{
\code{\link{count_syllables}}, \code{\link{count_words}}, \code{\link{tokenize_words}}
}
