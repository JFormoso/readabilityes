% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokenize-words.R
\name{tokenize_words}
\alias{tokenize_words}
\title{Tokenizar texto en palabras}
\usage{
tokenize_words(
  text,
  lang = syll_get_option("lang"),
  lowercase = TRUE,
  keep_accents = TRUE,
  strip_punct = TRUE,
  keep_hyphens = TRUE,
  keep_apostrophes = (lang == "ca"),
  remove_numbers = FALSE,
  strip_symbols = TRUE,
  flatten = FALSE
)
}
\arguments{
\item{text}{Vector de caracteres que contiene el texto a tokenizar.}

\item{lang}{Código de idioma: \code{"es"} (español, por defecto) o \code{"ca"}
(catalán). Si no se especifica, se usa \code{syll_get_option("lang")}.
En catalán activa \code{keep_apostrophes = TRUE} por defecto.}

\item{lowercase}{Lógico; si es \code{TRUE}, convierte todo el texto a
minúsculas antes de tokenizar.}

\item{keep_accents}{Lógico; si es \code{FALSE}, se eliminan las tildes.}

\item{strip_punct}{Lógico; si es \code{TRUE}, elimina la puntuación
(\code{\\p{P}}) antes de tokenizar.}

\item{keep_hyphens}{Lógico; si es \code{TRUE}, conserva los guiones dentro
de las palabras (p. ej., \emph{agradar-me}).}

\item{keep_apostrophes}{Lógico; si es \code{TRUE}, conserva los apóstrofes
dentro de los tokens (p. ej., \emph{l'Anna}, \emph{perdre'l}). Por
defecto es \code{TRUE} cuando \code{lang = "ca"} y \code{FALSE} en caso
contrario.}

\item{remove_numbers}{Lógico; si es \code{TRUE}, elimina los tokens
puramente numéricos.}

\item{strip_symbols}{Lógico; si es \code{TRUE}, elimina símbolos Unicode
(\code{\\p{S}}, p. ej., emojis, divisas).}

\item{flatten}{Lógico; si es \code{TRUE} y \code{text} tiene longitud 1,
devuelve un vector en lugar de una lista.}
}
\value{
\itemize{
\item Si \code{length(text) > 1}: lista donde cada elemento contiene los tokens
del elemento correspondiente de \code{text}.
\item Si \code{length(text) == 1} y \code{flatten = TRUE}: vector de caracteres.
\item En cualquier otro caso: lista de tokens.
}
}
\description{
Divide un vector de texto en tokens individuales de palabras. Soporta
reglas específicas por idioma: en catalán (\code{lang = "ca"}) los apóstrofes
se preservan por defecto ya que forman parte de contracciones léxicas
(p. ej., \emph{l'Anna}, \emph{perdre'l}).
}
\details{
La limpieza de puntuación usa la clase Unicode \code{\\p{P}}.
Los apóstrofes (\verb{'} y \verb{\\u2019}) se tratan por separado de la puntuación
general cuando \code{keep_apostrophes = TRUE}.
Si \code{strip_punct = TRUE} y \code{keep_hyphens = TRUE}, los guiones
internos se preservan.
}
\examples{
tokenize_words("Este es un texto de prueba, con tildes y números: 123.")
tokenize_words(c("Primera frase.", "Segona frase en català."), lang = "ca")
tokenize_words("l'Anna va a l'escola", lang = "ca", flatten = TRUE)
tokenize_words("agradar-me", lang = "ca", flatten = TRUE)

}
