% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avg_metrics.R
\name{letters_per_word}
\alias{letters_per_word}
\title{Letras promedio por palabra}
\usage{
letters_per_word(text, na_as_zero = FALSE, drop_empty = TRUE, ...)
}
\arguments{
\item{text}{Vector de caracteres con el/los texto(s) a analizar.}

\item{na_as_zero}{Lógico; si es \code{TRUE}, cuando \code{text} tiene \code{NA}
devuelve 0 en lugar de \code{NA}. Además, si un elemento no contiene palabras,
devuelve 0 en lugar de \code{NA}.}

\item{drop_empty}{Lógico; si es \code{TRUE}, descarta tokens vacíos antes de contar.}

\item{...}{Argumentos adicionales que se pasan a \code{\link{tokenize_words}}.}
}
\value{
Un vector numérico con el promedio de letras por palabra.
}
\description{
Calcula el promedio de \strong{letras} por palabra (sólo letras Unicode, excluye
dígitos y signos de puntuación) para cada elemento de \code{text}. La
tokenización se realiza con \code{\link{tokenize_words}}.
}
\details{
El conteo de letras se realiza usando la clase Unicode \code{\\p{L}}. Si
\code{strip_punct = TRUE} en \code{\link{tokenize_words}} (valor por defecto),
la mayor parte de la puntuación se elimina antes del conteo.
}
\examples{
letters_per_word("Hola mundo!")
letters_per_word(c("N° 123", "Palabra-compuesta"))

}
\seealso{
\code{\link{tokenize_words}}, \code{\link{count_words}}
}
